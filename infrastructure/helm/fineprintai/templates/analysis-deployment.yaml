{{- if .Values.analysis.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fineprintai.fullname" . }}-analysis
  labels:
    {{- include "fineprintai.labels" . | nindent 4 }}
    app.kubernetes.io/component: analysis
spec:
  {{- if not .Values.analysis.autoscaling.enabled }}
  replicas: {{ .Values.analysis.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "fineprintai.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: analysis
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
      labels:
        {{- include "fineprintai.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: analysis
        version: {{ .Values.analysis.image.tag | default .Chart.AppVersion }}
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "fineprintai.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: 60
      containers:
      - name: analysis
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        image: "{{ .Values.image.registry }}/{{ .Values.analysis.image.repository }}:{{ .Values.analysis.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.analysis.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.analysis.service.targetPort }}
          protocol: TCP
        - name: metrics
          containerPort: 9090
          protocol: TCP
        env:
        - name: NODE_ENV
          value: {{ .Values.config.nodeEnv | quote }}
        - name: LOG_LEVEL
          value: {{ .Values.config.logLevel | quote }}
        - name: ANALYSIS_PORT
          value: {{ .Values.analysis.service.targetPort | quote }}
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ include "fineprintai.fullname" . }}-secret
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: {{ include "fineprintai.fullname" . }}-secret
              key: redis-url
        - name: OLLAMA_BASE_URL
          value: "http://{{ include "fineprintai.fullname" . }}-ollama:{{ .Values.ollama.service.port }}"
        - name: QDRANT_URL
          value: "http://{{ include "fineprintai.fullname" . }}-qdrant:{{ .Values.qdrant.service.port }}"
        - name: MAX_CONCURRENT_ANALYSES
          value: "10"
        - name: ANALYSIS_TIMEOUT
          value: "300000"  # 5 minutes
        - name: REDIS_HOST
          value: "{{ include "fineprintai.fullname" . }}-redis-master"
        - name: REDIS_PORT
          value: "{{ .Values.redis.master.service.ports.redis }}"
        {{- if .Values.analysis.env }}
        {{- range $key, $value := .Values.analysis.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        {{- end }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 60
        resources:
          {{- toYaml .Values.analysis.resources | nindent 12 }}
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: var-cache
          mountPath: /var/cache
        - name: analysis-cache
          mountPath: /app/cache
        - name: config
          mountPath: /app/config
          readOnly: true
      volumes:
      - name: tmp
        emptyDir: {}
      - name: var-cache
        emptyDir: {}
      - name: analysis-cache
        emptyDir:
          sizeLimit: 2Gi
      - name: config
        configMap:
          name: {{ include "fineprintai.fullname" . }}-config
      {{- with .Values.analysis.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.analysis.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.analysis.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}